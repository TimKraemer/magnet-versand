<hr>
<div class="row">
	<div class="col-md-2">
		<%= image_tag(@choosenMagnet.image_url , size: "130")%>
	</div>
	<%= form_for :purchase do |f| %>
	<div class="col-md-2">
		<h3>Maße</h3>
			<% @attributes.attributes.each do |key,value| %>
			<%if value.is_a? String %>
			<div class="form-group">
				<div class="input-group">
					<%= f.text_field "attributes[#{value}]" , :placeholder => value ,class: "form-control" %>
					<span class="input-group-addon">mm</span>
				</div>
			</div>
			<% end %>
			<% end %>
	</div>

	<div class="col-md-2">
		<h3>Pole</h3>
			<div class="form-group">
				<% @poles.attributes.each do |key,value| %>
				<%if value.is_a? String %>
					<div class="radio">
						<%= radio_button("purchase", "Pole", value) %><%= value %>
					</div>
				<% end %>
				<% end %>
			</div>
			<%= check_box("purchase", "Kennzeichnen") %> Kennzeichnen?
	</div>

	<div class="col-md-2">
		<h3>Güte</h3>
			<div class="form-group">
				<%= f.select(:quality, ["1.02T < 180°C", "1.02T < 200°C", "1.08T < 180°C", "1.13T < 180°C"], {}, { :class => 'form-control form-striped' }) %>
				<option value=""></option>
			</div>         
	</div>
	<div class="col-md-2">
		<h3>Beschichtung</h3>
			<div class="form-group">
				<%= f.select(:coating, ["Keine", "Nickel", "Zink", "Silber", "Gold"], {}, { :class => 'form-control form-striped' }) %>
			</div>         
	</div>
	<div class="col-md-2">
		<h3>Warenkorb</h3>
		<div class = "input-group">
			<%= f.text_field :quantity, class: "form-control"%>
			<span class ="input-group-addon"> Stck </span>
		</div>
		<div class= "form-group">
			<%= button_tag(type: 'submit', class: "btn btn-block ") do %>
			<span class="glyphicon glyphicon-shopping-cart"></span> hinzufügen
			<% end %>
		</div>
		<% end %>

	</div>
